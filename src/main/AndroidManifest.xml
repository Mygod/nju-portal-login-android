<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="tk.mygod.nju.portal.login"
          android:versionCode="368" android:versionName="1.0.0.368">
    <uses-sdk android:minSdkVersion="16" android:targetSdkVersion="23" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_CONDITIONS"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_SETTINGS"/>
    <application android:name=".App" android:label="@string/app_name" android:theme="@style/Theme.Material">
        <activity android:name=".MainActivity" android:label="@string/app_name" android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <receiver android:name=".NetworkConditionsReceiver">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE"/>
                <action android:name="android.net.conn.NETWORK_CONDITIONS_MEASURED"/>
            </intent-filter>
        </receiver>
        <service android:name=".PortalManager" android:exported="false">
            <intent-filter>
                <action android:name="tk.mygod.nju.portal.login.PortalManager.START"/>
                <action android:name="android.net.conn.NETWORK_CONDITIONS_MEASURED"/>
                <action android:name="tk.mygod.nju.portal.login.PortalManager.STOP"/>
            </intent-filter>
        </service>
    </application>
</manifest>
