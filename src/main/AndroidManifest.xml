<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="tk.mygod.portal.helper.nju"
          android:versionCode="380" android:versionName="1.5.0">
    <uses-sdk android:minSdkVersion="16" android:targetSdkVersion="23" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.WRITE_SETTINGS"/>
    <application android:name=".App" android:label="@string/app_name" android:icon="@drawable/logo"
                 android:theme="@style/Theme.Material" android:allowBackup="true" android:fullBackupContent="true">
        <activity android:name=".MainActivity" android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".PortalActivity" android:exported="true" android:label="@string/portal_activity_name"
                  android:launchMode="singleTask"/>
        <service android:name=".NetworkMonitor"/>
        <receiver android:name=".NetworkMonitorListener">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE"/>
            </intent-filter>
        </receiver>
        <receiver android:name=".NoticeManager" android:exported="false">
            <intent-filter>
                <action android:name="tk.mygod.portal.helper.nju.NoticeManager.MARK_AS_READ"/>
                <action android:name="tk.mygod.portal.helper.nju.NoticeManager.VIEW"/>
            </intent-filter>
        </receiver>
    </application>
</manifest>
